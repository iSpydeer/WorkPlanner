package com.ispydeer.WorkPlanner.entities.user.role;

/**
 * Enum representing roles a user can have within the application.
 * Each role defines the permissions and access levels associated with a user.
 */
public enum Role {
    USER, ADMIN
}
